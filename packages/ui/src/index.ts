/**
 * @creative-lab/ui
 *
 * Shared UI package. Scaffolding only (BUILD-000A).
 *
 * Single source of truth for reusable interface primitives, foundations,
 * tokens, icons, components, layouts, hooks, and providers.
 *
 * Applications must consume this package rather than duplicating UI.
 * No React component implementations in BUILD-000A.
 */
export {};
