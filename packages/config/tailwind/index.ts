/**
 * Shared Tailwind CSS preset scaffold for Creative Lab.
 * BUILD-000A — structure only; no design-system tokens wired yet.
 *
 * Consumers may extend this object when Tailwind is introduced in apps.
 */
export const tailwindPreset = {
  theme: {
    extend: {},
  },
  plugins: [] as unknown[],
};

export default tailwindPreset;
