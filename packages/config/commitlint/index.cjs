/**
 * Shared Commitlint preset scaffold for Creative Lab (BUILD-000A).
 * Activate when commitlint is installed at the workspace root.
 */
module.exports = {
  extends: [],
  rules: {
    "header-max-length": [2, "always", 100],
  },
};
